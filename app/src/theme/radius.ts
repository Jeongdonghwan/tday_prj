/** 디자인 토큰 — 라운드 (스펙 §4: 카드 16~18, 버튼 12~14, 칩 999) */
export const radius = {
  card: 16,
  cardLg: 18,
  button: 13,
  input: 14,
  chip: 999,
} as const;

/** 공통 여백 스케일 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
} as const;
