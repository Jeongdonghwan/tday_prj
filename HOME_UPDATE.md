# 오늘연애 홈 신설 & 탭 재구성 스펙 (HOME_UPDATE.md)

이전 디자인·기능 패치(아바타, 피드 카드, 오늘의 질문, 연애 이슈, 심리테스트존)는 **이미 반영 완료된 전제**의 후속 작업이다.
이번 작업은 IA 개편 하나: **홈 화면 신설 + GNB 재구성 + 기존 컴포넌트 재배치.**
기능 로직·API는 변경하지 않는다 — 이미 구현된 오늘의 질문·연애 이슈·인기글·테스트존을 새 위치로 옮기고 홈에서 조합할 뿐이다.

---

## 1. GNB 재구성

`홈 | 커뮤니티 | 연애이슈 | 마이` 4탭.

- 기존 "피드 = 첫 화면" 구조에서 커뮤니티를 별도 탭으로 분리, 앱 첫 화면은 새 홈.
- 글쓰기 FAB는 커뮤니티 탭에 유지.
- 활성 탭 filled + ink / 비활성 아웃라인 + gray (기존 아이콘 세트 스타일 유지). 신규 필요 아이콘: 홈, 연애이슈(뉴스형) 2종 — 기존 세트와 동일한 스트로크 굵기·둥근 조인으로 제작.
- 딥링크·푸시 랜딩 경로가 탭 인덱스/라우트명에 의존하고 있으면 전부 갱신할 것 (탭 개편의 단골 버그).

## 2. 새 홈 화면 (위 → 아래)

### 2-1. 오늘의 질문 카드
- 기존 커뮤니티 피드 상단의 질문 카드를 **홈 최상단으로 이동**. 커뮤니티 탭에서는 제거 (중복 노출 금지).
- 컴포넌트·로직 변경 없음, 마운트 위치만 이동. 활성 질문 없으면 미노출 규칙 유지.

### 2-2. 퀵메뉴 그리드
- 4열 × 2행, 8항목. 에셋: `assets/quickmenu/qm_01~08.svg` (틴트 라운드 배경 포함 완성형, 48px 표시).
- 라벨 11.5~12/600, 아이콘 아래 6px. 탭 시 스케일 다운(0.96) 프레스 효과.

| 에셋 | 라벨 | 이동 |
|---|---|---|
| qm_01_test | 연애유형테스트 | 테스트존 WebView 스크린 |
| qm_02_love | 연애고민 | 커뮤니티(해당 카테고리 필터) |
| qm_03_wedding | 예비부부·결혼준비 | 〃 |
| qm_04_money | 재테크고민 | 〃 |
| qm_05_work | 직장·커리어 | 〃 |
| qm_06_family | 가족·인간관계 | 〃 |
| qm_07_chat | 일상잡담 | 〃 |
| qm_08_all | 전체 게시판 | 커뮤니티(전체) |

- **기존 카테고리 체계가 위 6개와 다르면 기존 체계 기준으로 매핑/선별**하고 결과를 DECISIONS.md에 기록. 라벨과 아이콘 매칭이 어긋나는 항목은 가장 가까운 아이콘 사용.

### 2-3. 지금 뜨는 글 TOP5
- 최근 24h `조회×1 + 댓글×5 + 공감×3` 가중치 상위 5. 매 요청 계산 금지 — 10분 주기 집계 캐시(기존 배치 구조 있으면 얹고, 없으면 단순 캐시 테이블 신설).
- 행: 순위 숫자(rose 700) + 제목 1줄 ellipsis + 댓글 수. 탭 → 글 상세. "더보기" → 커뮤니티 인기순.

### 2-4. 오늘의 연애이슈 프리뷰
- 활성 이슈 1건 요약형 카드: 라벨 "오늘의 연애이슈" + 제목 + "투표 참여 n명". 탭 → 연애이슈 탭.
- 활성 이슈 없으면 섹션 미노출.

## 3. 연애이슈 탭

- 상단: 오늘의 이슈 카드 풀사이즈 (기존 구현된 이슈 카드 그대로 — 제목·요약·출처 아웃링크·2지선다 투표·댓글 진입).
- 아래: **지난 이슈 아카이브 리스트** — 날짜 · 제목 · 최종 투표 결과 미니 바(승리 측 % 표시) · 댓글 수. 탭 → 이슈 상세 (마감 투표는 결과만, 댓글은 계속 가능).
- 기존에 이슈가 피드 헤더 카드로 노출되고 있었다면 그 노출은 제거 — 이슈는 전용 탭 + 홈 프리뷰로만.

## 4. 적용 순서

1. GNB 4탭 골격 + 라우팅 재구성 (홈은 빈 스캐폴드) → 커밋
2. 홈 섹션 조립: 질문 카드 이동 → 퀵메뉴 → 인기글 → 이슈 프리뷰 → 커밋
3. 연애이슈 탭 (오늘 이슈 + 아카이브) → 커밋
4. 딥링크·푸시 랜딩 경로 갱신 + 전체 테스트 통과 확인 → 커밋
- 확인 케이스: 질문 유무 × 이슈 유무 4조합에서 홈이 자연스럽게 접히는지 / 퀵메뉴 8개 이동 경로 / 푸시 탭 랜딩.
- 스타일 수치·에셋 임의 변경 금지. 애매한 판단은 DECISIONS.md 기록 후 진행.
